name: testing wf 
env:
  delete_env_1: true

on:
  schedule:
    - cron: ""
jobs:
  delete1:
    if: ${{ env.delete_env_1 == 'true' }}
    runs-on: windws-latest
    environment: Delete1
    steps:
    - name: execute powershell and take argumnets
      uses: Azure/powershell@v1
      with:
        inlineScript: echo ${{ vars.test1 }}
        azPSVersion : 'latest'
    
